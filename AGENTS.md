# Versioning

- Keep the current version while implementing and testing changes. Do not bump it for individual fixes or local installs.
- Bump the version only when directly proceeding with a user-requested publish.
- The current development version is 0.0.2.
